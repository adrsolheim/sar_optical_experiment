## (Linux) Pytorch, no CUDA
```
conda create --name torch
conda install -c conda-forge rasterio jupyter-lab matplotlib
conda install notebook
conda install -c pytorch pytorch torchvision torchaudio cpuonly
```
